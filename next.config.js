/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "utfs.io",
            "avatars.githubusercontent.com"
        ]
    }
}

module.exports = nextConfig
